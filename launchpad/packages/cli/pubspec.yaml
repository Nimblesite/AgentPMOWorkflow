name: launchpad_cli
description: CLI for Launchpad - macOS launchd manager
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.0
  launchpad_core:
    path: ../core

dev_dependencies:
  test: ^1.24.0
