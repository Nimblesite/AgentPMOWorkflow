---
layout: base.njk
title: "Get Started — Install Agent PMO in Minutes"
description: "Set up Agent PMO in minutes. One command installs everything — .NET SDK, gh CLI, the PMO dashboard, and the scheduled refresh. Works on macOS, Linux, and Windows."
---

<section class="page-hero" aria-label="Page header">
  <div class="page-hero-inner">
    <div class="hero-badge">Quick Setup</div>
    <h1>One command.<br>Running in minutes.</h1>
    <p class="tagline">
      Clone the repo and run <code>make setup</code>. It handles .NET, the gh CLI, the dashboard,
      and the scheduled refresh. macOS, Linux, and Windows supported.
    </p>
  </div>
</section>

<!-- ── Install Banner ─────────────────────────── -->
<section class="install-banner" aria-label="One-line install command">
  <div class="install-banner-inner">
    <div class="fade-in">
      <p class="install-tagline">Copy. Paste. Done.</p>
      <div class="install-block" role="group" aria-label="Install command">
        <div class="install-cmd-wrap">
          <code class="install-cmd" id="install-cmd-display">git clone https://github.com/Nimblesite/AgentPMOWorkflow.git &amp;&amp; cd AgentPMOWorkflow &amp;&amp; make setup</code>
          <button class="copy-btn" aria-label="Copy install command" id="copy-btn" title="Copy to clipboard">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
            <span id="copy-label">Copy</span>
          </button>
        </div>
      </div>
      <p class="install-note">Works on macOS, Linux, and Windows. <code>make setup</code> auto-detects your OS and installs the .NET SDK and gh CLI if they're missing.</p>
    </div>
  </div>
</section>

<section class="section consulting-cta" aria-label="Get Nimblesite help with setup">
  <div class="section-inner">
    <div class="consulting-cta-inner">
      <div>
        <div class="section-label">Team Rollout</div>
        <h2>Need this across a real engineering team?</h2>
        <p>Nimblesite can audit your current DevEx, standardise the repos that matter, install quality gates, and help your team adopt AI coding agents without creating review or CI chaos.</p>
      </div>
      <div class="consulting-cta-actions">
        <a href="{{ site.consultingUrl }}" class="btn btn-primary" target="_blank" rel="noopener noreferrer">Book a DevEx call →</a>
        <a href="{{ site.consultingUrl }}" class="btn btn-outline" target="_blank" rel="noopener noreferrer">See consulting services</a>
      </div>
    </div>
  </div>
</section>

<!-- ── Prerequisites ──────────────────────────── -->
<section class="section" aria-label="Prerequisites">
  <div class="section-inner">
    <div class="fade-in">
      <div class="section-label">What you need</div>
      <h2 class="section-heading">Prerequisites</h2>
      <p class="section-subheading">
        The setup script handles most of this automatically. But here's what you're working with.
      </p>
    </div>

    <div class="prereq-grid fade-in delay-1">
      <div class="prereq-item">
        <span class="prereq-icon" aria-hidden="true">💻</span>
        <div>
          <h3>macOS, Linux, or Windows</h3>
          <p>macOS 12+ (Homebrew recommended), Debian/Ubuntu/Fedora Linux, or Windows 10+ (winget/choco). The setup script detects your OS.</p>
        </div>
      </div>
      <div class="prereq-item">
        <span class="prereq-icon" aria-hidden="true">🔑</span>
        <div>
          <h3>GitHub account with CLI auth</h3>
          <p>The dashboard uses <code>gh</code> to fetch PR and CI status. Run <code>gh auth login</code> once after install.</p>
        </div>
      </div>
      <div class="prereq-item">
        <span class="prereq-icon" aria-hidden="true">📁</span>
        <div>
          <h3>Repos under <code>~/Documents/Code/</code></h3>
          <p>The dashboard scans this directory. If your repos live elsewhere, edit <code>config.json</code> after setup.</p>
        </div>
      </div>
      <div class="prereq-item">
        <span class="prereq-icon" aria-hidden="true">🟣</span>
        <div>
          <h3>.NET SDK (auto-installed)</h3>
          <p>The dashboard is an F# script. The setup script installs .NET 8 via Homebrew (macOS) or apt/script (Linux).</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── Step-by-step manual ────────────────────── -->
<section class="section surface-band" aria-label="Manual setup steps">
  <div class="section-inner">
    <div class="fade-in">
      <div class="section-label">Manual setup</div>
      <h2 class="section-heading">Step by step</h2>
      <p class="section-subheading">
        <code>make setup</code> does all of this automatically. But here's what's happening under the hood.
      </p>
    </div>

    <div class="setup-steps fade-in delay-1">

      <div class="setup-step">
        <div class="setup-step-num">1</div>
        <div class="prose">
          <h3>Clone the repo</h3>
          <p>Clone Agent PMO into your code directory.</p>
          <pre><code>git clone https://github.com/Nimblesite/AgentPMOWorkflow.git
cd AgentPMOWorkflow</code></pre>
        </div>
      </div>

      <div class="setup-step">
        <div class="setup-step-num">2</div>
        <div class="prose">
          <h3>Install .NET SDK</h3>
          <p>The dashboard is an F# script run via <code>dotnet fsi</code>. Install .NET 8+:</p>
          <pre><code># macOS (Homebrew)
brew install dotnet

# Ubuntu / Debian
sudo apt-get update &amp;&amp; sudo apt-get install -y dotnet-sdk-8.0

# Windows (winget)
winget install Microsoft.DotNet.SDK.8

# Or download from: https://dot.net</code></pre>
        </div>
      </div>

      <div class="setup-step">
        <div class="setup-step-num">3</div>
        <div class="prose">
          <h3>Install and authenticate gh CLI</h3>
          <p>The dashboard fetches PR and CI status via the GitHub CLI.</p>
          <pre><code># macOS
brew install gh

# Ubuntu / Debian
sudo apt install gh

# Windows
winget install GitHub.cli

# Authenticate (all platforms)
gh auth login</code></pre>
        </div>
      </div>

      <div class="setup-step">
        <div class="setup-step-num">4</div>
        <div class="prose">
          <h3>Run the report manually</h3>
          <p>Verify everything works before setting up the scheduler.</p>
          <pre><code>dotnet fsi dashboard/repo-report.fsx
# Opens dashboard/repo-report.html</code></pre>
          <p>If you see your repos in the report, you're good to go.</p>
        </div>
      </div>

      <div class="setup-step">
        <div class="setup-step-num">5</div>
        <div class="prose">
          <h3>Set up the scheduled refresh</h3>
          <p>The setup script handles this. Manual instructions:</p>
          <pre><code># macOS — launchd (runs every 3 minutes)
cp com.agentpmo.repo-report.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.agentpmo.repo-report.plist

# Linux — cron job
echo "*/3 * * * * cd $(pwd) &amp;&amp; dotnet fsi dashboard/repo-report.fsx" | crontab -

# Windows — Task Scheduler (PowerShell, admin)
# The setup-windows.ps1 script handles this automatically</code></pre>
        </div>
      </div>

      <div class="setup-step">
        <div class="setup-step-num">6</div>
        <div class="prose">
          <h3>Install the enforcement skill (optional)</h3>
          <p>To standardise your other repos with the Claude skill:</p>
          <pre><code>make install-skill-claude
# Copies agent-pmo skill into ~/.claude/skills/</code></pre>
          <p>Then in any repo: tell Claude to run the <strong>agent-pmo</strong> skill.</p>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- ── Config ────────────────────────────────── -->
<section class="section" aria-label="Configuration options">
  <div class="section-inner">
    <div class="fade-in">
      <div class="section-label">Configuration</div>
      <h2 class="section-heading">Tailor it to your setup</h2>
      <p class="section-subheading">
        Copy <code>dashboard/config.example.json</code> to <code>dashboard/config.json</code> to customise.
      </p>
    </div>

    <div class="fade-in delay-1">
      <pre><code>{
  "scanDir": "/Users/you/Documents/Code",
  "githubOwners": "your-github-username",
  "maxRepos": "20",
  "excludeRepos": "",
  "excludeAuthor": "",
  "excludeRepoDomain": ""
}</code></pre>

      <div class="callout callout-accent">
        <strong>Point <code>scanDir</code> at your code directory.</strong> Set <code>githubOwners</code> to your GitHub username so the dashboard can filter repos. Use <code>excludeRepos</code> to skip specific repos by name.
      </div>
    </div>
  </div>
</section>

<!-- ── Next steps ────────────────────────────── -->
<section class="section surface-band" aria-label="Next steps">
  <div class="section-inner">
    <div class="section-center fade-in">
      <div class="section-label">What's next</div>
      <h2 class="section-heading">You're set up. Now what?</h2>
    </div>

    <div class="feature-grid fade-in delay-1">
      <div class="feature-card">
        <span class="feature-card-icon" aria-hidden="true">📐</span>
        <h3>Standardise your repos</h3>
        <p>Run the <strong>agent-pmo</strong> Claude skill on each of your repos. Give every project the same interface so agents can navigate them without help.</p>
        <a href="/features/#standards" class="btn btn-outline btn-sm feature-card-action">Learn about standards →</a>
      </div>
      <div class="feature-card">
        <span class="feature-card-icon" aria-hidden="true">🔒</span>
        <h3>Run agents in parallel</h3>
        <p>Got a large codebase? Point several agents at the same working tree — no worktrees. TMC's file locks keep them from colliding.</p>
        <a href="/features/#parallelism" class="btn btn-outline btn-sm feature-card-action">How parallelism works →</a>
      </div>
      <div class="feature-card">
        <span class="feature-card-icon" aria-hidden="true">🎭</span>
        <h3>Enable TMC</h3>
        <p>Run the <a href="https://tmc-mcp.dev" target="_blank" rel="noopener noreferrer">Too Many Cooks</a> MCP server and connect each agent. They share locks, messages, and plans so concurrent edits never conflict.</p>
        <a href="/features/#tmc" class="btn btn-outline btn-sm feature-card-action">Learn about TMC →</a>
      </div>
    </div>
  </div>
</section>

<!-- ── CTA ───────────────────────────────────── -->
<section class="section cta-banner" aria-label="Call to action">
  <div class="section-inner">
    <div class="fade-in">
      <h2>Questions, issues, or a team rollout?</h2>
      <p>Agent PMO is open source. For organisational DevEx and AI development adoption, work with Nimblesite directly.</p>
      <div class="cta-actions">
        <a href="{{ site.consultingUrl }}" class="btn btn-primary" target="_blank" rel="noopener noreferrer">Work with Nimblesite →</a>
        <a href="{{ site.githubUrl }}/issues" class="btn btn-secondary" target="_blank" rel="noopener noreferrer">Open an Issue</a>
      </div>
    </div>
  </div>
</section>

<script defer>
document.addEventListener('DOMContentLoaded', function () {
  const display = document.getElementById('install-cmd-display');
  const copyBtn = document.getElementById('copy-btn');
  const copyLabel = document.getElementById('copy-label');

  if (copyBtn) {
    copyBtn.addEventListener('click', async () => {
      const text = display.textContent;
      try {
        await navigator.clipboard.writeText(text);
        copyBtn.classList.add('copied');
        copyLabel.textContent = 'Copied!';
        setTimeout(() => {
          copyBtn.classList.remove('copied');
          copyLabel.textContent = 'Copy';
        }, 2000);
      } catch (e) {
        // Fallback
        const ta = document.createElement('textarea');
        ta.value = text;
        ta.style.position = 'fixed';
        ta.style.opacity = '0';
        document.body.appendChild(ta);
        ta.select();
        document.execCommand('copy');
        document.body.removeChild(ta);
        copyLabel.textContent = 'Copied!';
        setTimeout(() => { copyLabel.textContent = 'Copy'; }, 2000);
      }
    });
  }
});
</script>
