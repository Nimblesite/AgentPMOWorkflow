---
layout: base.njk
title: "How It Works — The Parallel Agent Workflow"
description: "The Agent PMO cycle explained: Dashboard scanning, agent dispatch, quality gates, and the TMC conductor coordinating agents in a shared working tree."
---

<section class="page-hero" aria-label="Page header">
  <div class="page-hero-inner">
    <div class="hero-badge">The PMO Cycle</div>
    <h1>From spectator to<br>engineering director.</h1>
    <p class="tagline">
      Five steps. Running continuously across every project simultaneously.
      Here's exactly how Agent PMO breaks the serialisation trap.
    </p>
  </div>
</section>

<!-- ── Architecture Overview ──────────────────── -->
<section class="section" aria-label="System overview">
  <div class="section-inner">
    <div class="fade-in">
      <div class="section-label">Architecture</div>
      <h2 class="section-heading">Two components. One system.</h2>
      <p class="section-subheading">
        Agent PMO has two parts that work together: a real-time PMO Dashboard that tells you what's happening,
        and Repo Standards Enforcement that makes every project agent-ready.
      </p>
    </div>

    <div class="diagram-wrap fade-in delay-1">
      <div class="diagram diagram-img" role="img" aria-label="System architecture diagram">
        <img src="/assets/images/architecture.svg" alt="Agent PMO architecture: You (PMO Director) monitors the PMO Dashboard, which dispatches Agents A, B, and C into Standardised Repos" width="700" height="auto" loading="lazy" />
      </div>
    </div>
  </div>
</section>

<section class="section consulting-cta" aria-label="Apply this AI development workflow with Nimblesite">
  <div class="section-inner">
    <div class="consulting-cta-inner">
      <div>
        <div class="section-label">Apply The Process</div>
        <h2>Nimblesite can help your team run this workflow safely.</h2>
        <p>We assess your DevEx, standardise your repos, wire up CI and quality gates, and design AI agent workflows that keep humans in control.</p>
      </div>
      <div class="consulting-cta-actions">
        <a href="{{ site.consultingUrl }}" class="btn btn-primary" target="_blank" rel="noopener noreferrer">Book a DevEx call →</a>
        <a href="mailto:{{ site.salesEmail }}" class="btn btn-outline">Email Nimblesite</a>
      </div>
    </div>
  </div>
</section>

<!-- ── Step-by-step ────────────────────────────── -->
<section class="section" style="background: var(--color-surface); border-top: 1px solid var(--color-border);" aria-label="Step by step walkthrough">
  <div class="section-inner">
    <div class="section-center fade-in">
      <div class="section-label">The Five Steps</div>
      <h2 class="section-heading">The PMO loop in detail</h2>
    </div>

    <div class="how-steps">

      <div class="how-step fade-in">
        <div class="how-step-num">01</div>
        <div class="prose">
          <h3>Dashboard scans all repos</h3>
          <p>
            An F# script runs every 3 minutes via <code>launchd</code> (macOS) or <code>cron</code> (Linux).
            It scans every git repo under your code directory and collects CI status, open PRs, uncommitted changes,
            push status, and latest releases — then generates a self-contained HTML report. No server needed.
          </p>
          <div class="terminal">
            <div class="terminal-header">
              <span class="terminal-dot terminal-dot-red"></span>
              <span class="terminal-dot terminal-dot-yellow"></span>
              <span class="terminal-dot terminal-dot-green"></span>
              <span class="terminal-title">~ launchd · runs every 180 seconds</span>
            </div>
            <div class="terminal-body">
<pre><span class="terminal-prompt">$ </span><span class="terminal-cmd">dotnet fsi dashboard/repo-report.fsx</span>
<span class="terminal-ok">✓ Scanning ~/Documents/Code/ ...</span>
<span class="terminal-dim">  Found 24 repositories</span>
<span class="terminal-ok">✓ Fetching PR status for 24 repos ...</span>
<span class="terminal-ok">✓ Fetching CI status for 24 repos ...</span>
<span class="terminal-ok">✓ Report written → dashboard/repo-report.html</span>
<span class="terminal-dim">  Generated in 8.3s</span></pre>
            </div>
          </div>
        </div>
      </div>

      <div class="how-step fade-in delay-1">
        <div class="how-step-num">02</div>
        <div class="prose">
          <h3>You review the dashboard and prioritize</h3>
          <p>
            Open <code>repo-report.html</code> in a browser. The dashboard tells you — at a glance — exactly which projects
            need attention. No context-switching into each repo to investigate. That's minimal cognitive load.
          </p>
          <p>
            You're not managing tasks. You're allocating agent capacity. Where's the most value?
            Which CI failure is blocking a PR? Which project has no activity and needs a nudge?
          </p>
          <div class="callout">
            <strong>Key insight:</strong> The dashboard is your air traffic control screen. You're the controller, not the pilot. Agents are the pilots.
          </div>
        </div>
      </div>

      <div class="how-step fade-in delay-2">
        <div class="how-step-num">03</div>
        <div class="prose">
          <h3>Dispatch agents into standardised repos</h3>
          <p>
            Because every repo uses the same structure — same Makefile targets, same CI pipeline, same lint and format commands —
            an agent dropped into any project already knows how to run, test, and ship. No setup. No hand-holding.
          </p>
          <p>
            For monorepos or multi-service codebases, multiple agents work the same repo simultaneously in
            <strong>one working tree</strong> — no git worktrees (they're banned; agents corrupt them).
            <strong>Too Many Cooks</strong> coordinates them with advisory file locks: an agent claims a file
            before editing it, and everyone else sees the claim instantly.
          </p>
          <div class="terminal">
            <div class="terminal-header">
              <span class="terminal-dot terminal-dot-red"></span>
              <span class="terminal-dot terminal-dot-yellow"></span>
              <span class="terminal-dot terminal-dot-green"></span>
              <span class="terminal-title">~ TMC file locks for parallel agents</span>
            </div>
            <div class="terminal-body">
<pre><span class="terminal-dim"># Agent A claims a file before editing</span>
<span class="terminal-ok">[TMC]</span> agent-A locked: src/auth/login.ts

<span class="terminal-dim"># Agent B picks a different file — same repo, same tree</span>
<span class="terminal-ok">[TMC]</span> agent-B locked: src/api/routes.ts

<span class="terminal-dim"># Both work in parallel — locks prevent collisions</span>
<span class="terminal-ok">✓ Parallel workstreams active</span></pre>
            </div>
          </div>
        </div>
      </div>

      <div class="how-step fade-in">
        <div class="how-step-num">04</div>
        <div class="prose">
          <h3>TMC coordinates the agents</h3>
          <p>
            <strong>Too Many Cooks (TMC)</strong> is the MCP server every agent in a workspace connects to. When
            multiple agents are active at once, TMC keeps them out of each other's way — they register, broadcast
            a plan, lock files before editing, message each other, and release locks when done. Every lock and
            message is pushed live, so no agent edits a file another already holds.
          </p>
          <p>
            <a href="/features/#tmc" class="btn btn-outline btn-sm">Full TMC protocol →</a>
          </p>
        </div>
      </div>

      <div class="how-step fade-in delay-1">
        <div class="how-step-num">05</div>
        <div class="prose">
          <h3>Review and ship</h3>
          <p>
            By the time code reaches you, it has passed every automated quality gate:
            lint, type check, format, unit tests, integration tests, coverage thresholds, and CI.
          </p>
          <p>
            You're not the first line of defence — you're the <strong>final gate</strong>.
            Human review is for intent, architecture, and edge cases — not for checking whether the tests pass.
          </p>
          <div class="callout callout-accent">
            <strong>Coverage thresholds are monotonically increasing.</strong> They never regress. Each PR must maintain or improve coverage. Agents fight to meet the bar, not to lower it.
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- ── CTA ────────────────────────────────────── -->
<section class="section cta-banner" aria-label="Next steps">
  <div class="section-inner">
    <div class="fade-in">
      <h2>Ready to make your DevEx AI-ready?</h2>
      <p>Use Agent PMO as the blueprint, or work with Nimblesite to adapt the process to your team, codebase, and delivery constraints.</p>
      <div class="cta-actions">
        <a href="/get-started/" class="btn btn-primary">Get Started →</a>
        <a href="{{ site.consultingUrl }}" class="btn btn-secondary" target="_blank" rel="noopener noreferrer">Work with Nimblesite</a>
      </div>
    </div>
  </div>
</section>
