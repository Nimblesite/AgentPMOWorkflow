{
  "name": "agent-pmo-website",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "preview": "npx @11ty/eleventy --serve --port 8080"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
